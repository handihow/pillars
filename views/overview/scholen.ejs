<% include ../partials/school/header %>

<div class="ui container">
    <h2 class="ui center aligned header">
      <div>Progressie Invoer Scholen</div>
    </h2>
    <p>Controleer de progressie van invoer voor scholen binnen jouw bestuur. Groene indicatie betekent slechts dat er een begin gemaakt is met de invoer.
    Bijvoorbeeld, een groene indicatie wordt al gegeven als slechts 1 computer is ingegeven bij de sectie Hardware.</p>
    <!--make table with schools and progress-->
    <table class="ui celled small table">
      <thead>
        <tr>
          <th>Instellingsnaam</th>
          <th>Plaatsnaam</th>
          <th>Leerlingen</th>
          <th>Algemene info</th>
          <th>Hardware</th>
          <th>Digitale Leermiddelen</th>
          <th>Deskundigheid</th>
          <th>Organisatie</th>
          <th>Medewerkers</th>
        </tr>
      </thead>
      <tbody>
        <% scholen.forEach(function(school){ %>
            <tr>
              <td><%=school.instellingsnaam%></td>
              <td><%=school.plaatsnaam%></td>
              <td class="right aligned <% if(school.aantalLeerlingen){ %> positive <% } else { %> negative <% } %>" ><%=school.aantalLeerlingen%></td>
              <td class="center aligned"><% if(school.isIngevuldAlgemeneInformatie){ %> <i class="large green checkmark icon"></i> <% } %> </td>
              <td class="center aligned"><% if(school.isToegevoegdHardware){ %> <i class="large green checkmark icon"></i> <% } %> </td>
              <td class="center aligned"><% if(school.isToegevoegdSoftware){ %> <i class="large green checkmark icon"></i> <% } %> </td>
              <td class="center aligned"><% if(school.isIngevuldDeskundigheid){ %> <i class="large green checkmark icon"></i> <% } %> </td>
              <td class="center aligned"><% if(school.isIngevuldOrganisatie){ %> <i class="large green checkmark icon"></i> <% } %> </td>
              <td class="center aligned"><% if(school.isToegevoegdMedewerker){ %> <i class="large green checkmark icon"></i> <% } %> </td>
            </tr>
        <% }) %>
      </tbody>
    </table>
</div>

<% include ../partials/school/footer %>