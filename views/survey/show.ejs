<% include ../partials/header %>
<% include ../partials/main-menu %>
<% include ../partials/flash-message %>
<%if(schoolLevel){%>
  <% include ../partials/school-header %>
<%} else { %>
 <div class="ui container">
<%}%>
<div class="ui icon buttons">
  <button class="ui active icon button" id="infoButton"><i class="edit icon"></i></button>
  <button class="ui icon button" id="tableButton"><i class="table icon"></i></button>
  <button class="ui icon button" id="chartButton"><i class="chart pie icon"></i></button>
  <% if(survey.isCompetenceSurvey){%>
    <button class="ui icon button" id="analysisButton"><i class="chart bar icon"></i></button>
  <%}%>
</div>
<div class="ui divider"></div>

<div id="info" class="transition">
  <% include ./show/info %>
</div>

  <div id="table" class="transition hidden">
    <% include ./show/table %>
  </div>
  <div id="chart" class="transition hidden">
    <% include ./show/chart %>
  </div>
  <% if(survey.isCompetenceSurvey){%>
    <div id="analysis" class="transition hidden">
      <% include ./show/analysis %>
    </div>
  <%}%>
  <div id="surveyText" hidden><%= JSON.stringify(survey)%></div>
  <div id="surveyResults" hidden><%= JSON.stringify(surveyResults)%></div>
<%if(schoolLevel){%>
  <% include ../partials/school-footer %>
<% } else { %>
  </div>
<% }%>

<% include ../partials/footer %>